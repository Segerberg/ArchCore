{% extends "base.jinja2" %}

{% block content %}
{% include "preservation_planning/_submenu.jinja2" %}
<div class="flex-container">
   <div class="flex-item">
        <div class="card">
            <div class="card-header"><h3><a href="{{ url_for('preservation_planning.format_registry') }}"> {{_('Format Registry')}}</a></h3></div>
            <div class="card-content">
                <p><strong>Formats in registry:&nbsp;</strong>{{format_count}}</p>
            </div>
        </div>
   </div>
       <div class="flex-item">
        <div class="card">
            <div class="card-header"><h3><a href="{{ url_for('preservation_planning.format_registry') }}"> {{_('Format Registry')}}</a></h3></div>
            <div class="card-content">
                <p><strong>Formats in registry:&nbsp;</strong>{{format_count}}</p>
            </div>
        </div>
   </div>
</div>
{% endblock %}